name: baby_record
description: 宝宝成长记录 App
publish_to: 'none'
version: 3.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  intl: ^0.19.0
  fl_chart: ^0.69.0
  provider: ^6.1.0
  uuid: ^4.5.1
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
